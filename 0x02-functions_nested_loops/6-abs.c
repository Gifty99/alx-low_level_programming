#include "main.h"

/**
 * _abs - compute the _abs function
 *
 * @i: int type integer
 *
 * Decription: Compute the absolute value of an integer
 *
 * Return: absolute value of @i
 *
 */

int _abs(int i)

{

	if (i >= 0)

		return (i);

	else

		return (i * -1);

}
