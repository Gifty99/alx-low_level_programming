#include "main.h"

/**
 * _strlen - a function that returns the length of a string
 * @s: input string
 *
 * Return: nothing
 */

int _strlen(char *s)
{
	int length;

	for (length = 0; s[length] != '\0'; length++)

	return (length);

}
